1: the original_languaje_id column is innecesary in this context, all can be managed by languaje_id
2: not ideal to save special_features information like that, violates database principles and needs a complicated query to navigate it
3: not to use spaces or special characters in the rating column, by doing that we could simplify the code